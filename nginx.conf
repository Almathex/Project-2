events{}
http {
    upstream server {
        server manager1:5000;
        server manager1:80;
        server worker:5000;
        server worker:80;
    }
    server {
        location / {
            proxy_pass http://server;
        }
    }
}