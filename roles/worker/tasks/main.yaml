- name: Leave Docker Swarm
  docker_swarm:
    state: absent
    force: true

- name: "Join a swarm"
  docker_swarm:
    state: join
    remote_addrs: [ "manager:2377" ]  
    advertise_addr: "worker-1"
    join_token: "{{ hostvars['token']['value'] }}"