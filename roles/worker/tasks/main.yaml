- name: Leave Docker Swarm
  docker_swarm:
    state: absent
    force: true

- name: Join Docker Swarm - Worker 
  docker_swarm:
    state: join
    remote_addrs: 10.154.0.60  
    join_token: "{{ hostvars['join_token']['value'] }}"
    remote_addrs: [ "10.154.0.59:2377" ]